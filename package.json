{
  "name": "natroceutics-kb-os",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "sync": "node --env-file-if-exists=.env --env-file-if-exists=.env.local scripts/sync-airtable.js",
    "sync:stats": "npm run sync -- --stats"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "airtable": "0.12.2",
    "next": "14.2.3",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
